{
  "name": "operator-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "Operator — a self-driving browser that reasons about arbitrary websites.",
  "license": "MIT",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "npm run dev --workspace @operator/desktop",
    "build": "npm run build --workspace @operator/core && npm run build --workspace @operator/desktop",
    "build:core": "npm run build --workspace @operator/core",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspace @operator/core",
    "package": "npm run build:core && npm run package --workspace @operator/desktop"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
